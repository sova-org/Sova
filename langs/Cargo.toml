[package]
name = "langs"
version.workspace = true
edition = "2024"
publish = false

[build-dependencies]
lalrpop = "0.23.1"

[features]
rhai = ["dep:rhai"]
lua = ["dep:mlua"]

[dependencies]
sova_core = { path = "../core", package = "core" }
lalrpop-util = { workspace = true }
pest = { workspace = true }
pest_derive = { workspace = true }
rhai = { version = "1.26.1", features = ["internals", "no_module", "no_time"], optional = true }
mlua = { version = "0.12.1", features = ["luau"], optional = true }
rand = { workspace = true }
serde = { workspace = true, features = ["rc"] }
serde_json = { workspace = true }

[dev-dependencies]
rusty_link = "0.4.9"
regex = "1"
