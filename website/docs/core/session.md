# Session

Everytime the Sova Core runs, it manages a **session**. A session is everything that is needed to run Sova: the current state of the engine, the devices connected, the languages loaded, etc.